[package]
name = "statikmanager-web-tauri"
version = "0.0.1"
description = "Desktop-Hülle für StatikManager Web"
authors = ["StatikManager"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-dialog = "2"
